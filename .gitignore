# Secrets — real keys live in `fly secrets` and an untracked local .env only
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/

# Node / console build
node_modules/
dist/
*.tsbuildinfo

# Remotion render output — artifacts of record live in Postgres, not the tree
out/
*.mp4
preview/

# Sampled brand docs, narrations and reports — the findings go in evidence.md, the draws do
# not. Glob, because comparing prompt versions means one directory per version.
samples*/

# Working notes, deliberately untracked (see .claude/DECISIONS.md header)
.claude/DECISIONS.md
.claude/plans/

# macOS
.DS_Store
