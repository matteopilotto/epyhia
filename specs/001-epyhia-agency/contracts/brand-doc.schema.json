{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://epyhia.dev/contracts/brand-doc.schema.json",
  "title": "Brand document",
  "description": "The parameterisation layer (DESIGN.md 5.3, FR-009/FR-010). Written by the Strategist from the brief; read by every other agent. Every agent except the Reviewer reads THIS and never the raw brief (FR-011). Structure, not prose, because three consumers need fields: the Web Builder needs real hex values, the Reviewer needs checkable do/don't items, and the deploy probe needs `name` as a queryable field. The schema is fixed; the contents are generated per client and the schema MUST NOT change to accommodate one.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "descriptor",
    "positioning",
    "palette",
    "type",
    "motion_language",
    "composition_archetype",
    "video_archetype",
    "voice",
    "composition_plan"
  ],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "The string the deploy probe asserts is present in the response body, read from this row at verify time (DESIGN.md 4.5, FR-018). Never a literal in source."
    },
    "descriptor": { "type": "string", "minLength": 1 },
    "positioning": { "type": "string", "minLength": 1 },
    "palette": {
      "type": "object",
      "additionalProperties": false,
      "required": ["bg", "fg", "accent", "muted"],
      "description": "Concrete hex values chosen by the Strategist. Specificity is the anti-slop lever (DESIGN.md 6.3); an under-specified palette is what produces the generic result.",
      "properties": {
        "bg": { "$ref": "#/$defs/hex" },
        "fg": { "$ref": "#/$defs/hex" },
        "accent": { "$ref": "#/$defs/hex" },
        "muted": { "$ref": "#/$defs/hex" }
      }
    },
    "type": {
      "type": "object",
      "additionalProperties": false,
      "required": ["display", "body"],
      "properties": {
        "display": { "type": "string", "minLength": 1 },
        "body": { "type": "string", "minLength": 1 }
      }
    },
    "motion_language": {
      "type": "string",
      "minLength": 1,
      "description": "Named, e.g. 'mechanical', 'editorial drift'."
    },
    "composition_archetype": {
      "type": "string",
      "description": "Selected from the site section-layout library, not authored."
    },
    "video_archetype": {
      "type": "string",
      "description": "Selected from the Remotion archetype library (DESIGN.md 6.4). The Strategist selects; the Marketer fills props."
    },
    "voice": {
      "type": "object",
      "additionalProperties": false,
      "required": ["adjectives", "do", "dont"],
      "properties": {
        "adjectives": { "type": "array", "minItems": 1, "items": { "type": "string" } },
        "do": { "type": "array", "items": { "type": "string" } },
        "dont": { "type": "array", "items": { "type": "string" } }
      }
    },
    "composition_plan": {
      "type": "array",
      "minItems": 1,
      "description": "Section-level plan handed to the Web Builder so it composes rather than invents a page (DESIGN.md 6.3). Contains layout intent only — never copy, never a price, never a claim.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["section", "layout", "intent"],
        "properties": {
          "section": { "type": "string" },
          "layout": { "type": "string" },
          "intent": { "type": "string" }
        }
      }
    }
  },
  "$defs": {
    "hex": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" }
  }
}
